<div>
  <h4 class="{{css_class}}">{{name}}</h4>
  <div id="language"> <!-- TODO make 'language' a class, not an id. -->
    <div id="use">
      <h4>use</h4>
      <p><span class="used">io</span> <span>parlare</span></p>
<!-- inside here are many '<p>...<span>..</span> ... </p>'s. -->
    </div>
    <div id="unsolved">
      <h4>unsolved</h4>
      <!-- inside here are many '<p>...<span>..</span> ... </p>'s. -->
      <p><span class="blank1"> </span> parlare</p>
</div>
    <textarea name="ok" placeholder="agguingi le tue sentenze.."></textarea>
    <div id="solved">
      <h4>solved</h4>
      <!-- inside here are many '<p>...<span>..</span> ... </p>'s. -->
      No sentences solved yet.
    </div>
  </div>
</div>

